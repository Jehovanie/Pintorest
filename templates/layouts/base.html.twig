<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <!-- À supprimé lors de la deploiment -->
    <link rel="stylesheet" href="{{asset('style/css/bootstrap.css')}}">
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: 0;
        }

        body {
            display: block;
            min-width: 100vw;
            min-height: 100vh;
            overflow-x: hidden;
        }

        main {
            display: block;
            min-height: 70vh;
            overflow: hidden;
        }

        .alert {
            display: block;
            text-align: center;
            align-items: center;
            margin: 0;
            border: none;
            border-radius: 0;
        }

        .alert-success {
            background-color: green;
            color: white;
        }

        .alert-info {
            background-color: rgb(10, 7, 202);
            color: white;
        }

        .alert-error {
            background-color: red;
            color: black;
        }
    </style>

    {% endblock %}

    {% block javascripts %}

    {{ encore_entry_script_tags('app') }}

    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous">
        </script>

    <!-- À supprimé lors de la deploiment -->
    <script src="{{asset('style/js/bootstrap.css')}}"></script>
    {% endblock %}


</head>

<body>
    {{ include('layouts/partials/_flash_message.html.twig')}}
    {{ include('layouts/partials/_nav.html.twig')}}

    <main>
        {% block body %}{% endblock %}
    </main>

    {{ include('layouts/partials/_footer.html.twig')}}

</body>

</html>