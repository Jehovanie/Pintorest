{% extends 'base.html.twig' %}

{% block title %}Pintorest : Modifier Pin {% endblock %}

{% block body %}
<div class="container">
    <div class="card">
        <div class="card-title">
            <h4>Modifier cette Pin </h4>
        </div>
        <div class="card-body">
            {{ form_start(formulaire)}}
            {{ form_widget(formulaire) }}
            <input type="submit" value="Persiste Modification">
            {{ form_end(formulaire)}}
        </div>
        <div class="card-footer">
            <h5> <a href="{{path("app_home")}}">Annuler</a></h5>
        </div>
    </div>
</div>
{% endblock %}